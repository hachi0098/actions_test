name: GitHub Actions Demo
on: 
  push:
    branches:
      - issue*
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - name: Check if all reviews are approved
        id: check-reviews
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # Get the pull request number
          pull_request_number=$(jq --raw-output .pull_request.number "$GITHUB_EVENT_PATH")

          # Get the repository name and owner
          repository=$(jq --raw-output .repository.full_name "$GITHUB_EVENT_PATH")

          # Get the state of all reviews for the pull request
          reviews=$(curl -s -H "Authorization: $GITHUB_TOKEN" "https://api.github.com/repos/$repository/pulls/$pull_request_number/requested_reviewers")
          echo $reviews
          echo ↑

          # requested_reviewers=$(curl -s -H "Accept: application/vnd.github+json" -H "Authorization: Bearer $GITHUB_TOKEN" -H "X-GitHub-Api-Version: 2022-11-28" https://api.github.com/repos/$repository/pulls/$pull_request_number/requested_reviewers)
          # echo $requested_reviewers

          # # Check if all reviews have state "APPROVED"
          # approved=true
          # for review in $(echo "$reviews" | jq -r '.[].state'); do
          #   if [ "$review" != "APPROVED" ]; then
          #     approved=false
          #     break
          #   fi
          # done
          # # If all reviews are approved, continue with next step
          # if [ "$approved" = true ]; then
          #   echo ::set-output name=approved::true
          # else
          #   echo ::set-output name=approved::false
          # fi
      - name: check status
        if: steps.check-reviews.outputs.approved != 'true'
        run: exit 1  
      - run: echo "🎉 The job was automatically triggered by a ${{ github.event_name }} event."