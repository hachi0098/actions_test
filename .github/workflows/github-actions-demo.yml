name: GitHub Actions Demo
on: 
  push:
    branches:
      - issue*
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - name: Check if all reviews are approved
        id: check-reviews
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # Get the pull request number
          pull_request_number=$(echo $GITHUB_REF | sed -e 's/[^0-9]//g')
          echo $pull_request_number

          # Get the repository name and owner
          repository=$(jq --raw-output .repository.full_name "$GITHUB_EVENT_PATH")
          echo $repository

          # Get the state of all reviews for the pull request
          reviews=$(curl -s -H "Authorization: token $GITHUB_TOKEN" "https://api.github.com/repos/$repository/pulls/$pull_request_number/reviews")
          echo $reviews

          reviewers=$(curl \
            -H "Accept: application/vnd.github+json" \
            -H "Authorization: Bearer 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" \
            -H "X-GitHub-Api-Version: 2022-11-28" \
            https://api.github.com/repos/hachi0098/actions_test/pulls/5/requested_reviewers)
          
          echo $reviewers

          # Check if all reviews have state "APPROVED"
          approved=true
          for review in $(echo "$reviews" | jq -r '.[].state'); do
            if [ "$review" != "APPROVED" ]; then
              approved=false
              break
            fi
          done
          # If all reviews are approved, continue with next step
          if [ "$approved" = true ]; then
            echo ::set-output name=approved::true
          else
            echo ::set-output name=approved::false
          fi
      - name: Exit if reviews not approved
        if: steps.check-reviews.outputs.approved != 'true'
        run: exit 1  
      - run: echo "🎉 The job was automatically triggered by a ${{ github.event_name }} event."


      curl -H "Authorization: token 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" https://api.github.com/repos/hachi0098/actions_test/pulls/5/reviews

https://api.github.com/repos/hachi0098/actions_test/pulls/5/reviews

      curl \
        -H "Accept: application/vnd.github+json" \
        -H "Authorization: Bearer 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" \
        -H "X-GitHub-Api-Version: 2022-11-28" \
        https://api.github.com/repos/hachi0098/actions_test/pulls/5/requested_reviewers